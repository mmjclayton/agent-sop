# Review — P84/P85 [Pass-one remediation: five silent failures, two dead gates, check-ID collisions]

**Date:** 2026-08-03
**Agent:** solo
**Reviewer agent:** code-reviewer
**Files changed:** 16
**Lines changed:** +369 / -58
**Commits:** 4473da2, 55b3cea

## Summary

`4473da2` fixes four `pipefail`/`errexit`-class silent failures (`refresh-rollup.sh` hardcoded `CLAUDE.md` target + unguarded title `grep`, `validate-state-transitions.sh` `resolve_before()` bare `return`, `run-benchmark.sh` unguarded `ls`), extracts `detect-trackers.sh` to fix an unreachable `/update-sop` Step 11 hard block, ports `.expect-stdout` assertions into the drift-fixtures harness with a `VALIDATOR` override, adds filename-collision detection to `migrate-to-multi-agent.py`, and caveats three uncaveated "33%" benchmark citations plus a missing `sop-checker.md` `S4` heading and a stale slash-command count. `55b3cea` renames two sets of duplicate compliance-check IDs (`M1`-`M4`→`FM1`-`FM4`, `R1`-`R3`→`RP1`-`RP3`) and adds a `docs/recent-work/` row to the SOP's Section 2 file-ownership table.

Severity: HIGH

## Findings

- `.claude/commands/update-sop.md:562` — Step 11's reconciliation hard block reads `for tracker in $(bash scripts/detect-trackers.sh); do`. Unquoted command substitution word-splits on IFS, so a tracker path containing a space is torn into fragments that don't exist as files. Reproduced: seeded a project with a tracker at `docs/audit findings.md` carrying `## A1 finding [OPEN]`; the loop produced tokens `docs/audit` and `findings.md`, `grep -qE ... "$tracker"` failed on both (no such file), and the block never fired even though A1 is genuinely still `[OPEN]`. This is the same "gate fails open" class this pass fixed everywhere else. It isn't strictly new — the pre-fix code already wrote `for tracker in $(detect_trackers); do` calling an undefined function, so the whole step was 100% dead before this change — but the fix makes the step live for the first time and carries the latent quoting bug live with it. Step 3b's use of the same script (`.claude/commands/update-sop.md:270`) is unaffected — it just prints the list for the agent to read, no shell iteration.
  Fix: `while IFS= read -r tracker; do ... done < <(bash scripts/detect-trackers.sh)`.

- `scripts/refresh-rollup.sh:35-47` (`resolve_rollup_file`) — when both `docs/RECENT-WORK.md` and `CLAUDE.md` carry the `<!-- recent-work-rollup:start -->` sentinel (a reachable mid-migration state — the docstring at `:5-8` describes exactly this cutover, and it requires a human to remember to delete the old sentinel from `CLAUDE.md` by hand), the function silently picks `docs/RECENT-WORK.md` and never touches `CLAUDE.md` again, with no warning that a second live-looking rollup now exists. Reproduced: seeded both files with the sentinel and distinct placeholder bodies; after running the script, `docs/RECENT-WORK.md` refreshed correctly but `CLAUDE.md`'s block was byte-identical to the stale seed, with no stderr output flagging the second candidate. `docs/sop/compliance-checklist.md:91` (C13) and `:316` (M5) both accept "either location," so the orphaned `CLAUDE.md` block is invisible to the compliance checker too — anyone reading `CLAUDE.md` directly sees a plausible but dead rollup.
  Fix: after the `for` loop finds its match, check whether the *other* candidate also has the sentinel and print a warning naming both files if so.

- `docs/sop/compliance-checklist.md:142-143` — `B12` (`Every [DEFERRED] item states a reopen trigger`) still precedes `B11` (`State-transition validator present`), out of numeric order. `Backlog.md:1567` (P76, currently `[OPEN]`) names "`B11`/`B12` in numeric order" as one of four acceptance criteria for the ID-collision cleanup this pass performed. The other three are met (grepped the whole file: 94 rows, 0 duplicate IDs; `README.md`/`sop-checker.md` citations checked below; row count unchanged so the 85/94 totals stay correct) but this one wasn't touched, and `Backlog.md:1744-1750` (the P85 write-up for `55b3cea`) doesn't note it as deferred — the gap is only discoverable by re-reading P76's original acceptance criteria against the current file.

- `docs/sop/claude-agent-sop.md` — P85's own stated rationale (`Backlog.md:1747`) is that "`docs/recent-work/` appeared in neither the Section 2 ownership table nor the Section 7 update-trigger table." Only Section 2 was fixed (`claude-agent-sop.md:158`, confirmed present and correctly worded). Section 7 "Update Triggers" (`:436-451`) still has no row for `docs/recent-work/`; its "Session ends" row (`:451`) reads `` `project_resume.md` (overwrite with snapshot), `docs/agent-memory.md` (In-Flight) `` — no mention of writing the recent-work entry or refreshing the rollup, and it still cites the legacy unsuffixed `project_resume.md` rather than the per-agent `project_resume_<agent-id>.md` used everywhere else in the file (e.g. `:158-159`, `:127`). Half of the diagnosed ownership gap persists, unflagged.

## Notes (optional)

Verified clean by direct testing, not just inspection:
- `resolve_before()` fix (`scripts/validate-state-transitions.sh:613-624`): reproduced the pre-fix bug against the pre-`4473da2` script (`--before-file <missing-path>` → exit 1, 0 bytes stdout, 0 bytes stderr) and confirmed the post-fix script prints the "no before-state ... Skipping" message and exits 0 in the same scenario.
- `refresh-rollup.sh` target resolution: tested explicit-arg override, `docs/RECENT-WORK.md`-only, `CLAUDE.md`-only, neither (correct error + exit 1), and an explicit-arg path containing a space — all correct except the dual-sentinel case above.
- `refresh-rollup.sh` title fallback (`:81-86`): an entry with no `# ` heading now degrades to `(untitled)` instead of killing the script under `pipefail`.
- `migrate-to-multi-agent.py` collision detection: reproduced a same-date/same-title Recent Work collision (aborts, nothing written, `--dry-run` also aborts), a cross-category collision between a superseded Decision and an Archived entry resolving to the same `decisions/archive/` path (caught), and a normal multi-entry run with no collisions (writes all 5 files correctly). The `superseded_date`-normalisation-before-precheck fix for archived entries (`migrate-to-multi-agent.py:401-406`) is correctly ordered ahead of the `planned` list construction (`:411-416`).
- `docs/benchmark/drift-fixtures/run-tests.sh` `VALIDATOR` override: pointed it at a stub that reproduces correct exit codes but prints nothing (the P73 shape) — the new `.expect-stdout` assertion correctly fails the `illegal-mismatch` fixture with "exit 1 correct, but expected stdout line missing," proving the harness now discriminates a silently-failing validator as claimed.
- Both fixture suites run green as claimed: `state-transition-fixtures/run-tests.sh` 17/17, `drift-fixtures/run-tests.sh` 5/5.
- No stale citations to the renamed `M1`-`M4` (feature-map) or `R1`-`R3` (resume) IDs found outside `Backlog.md`/`docs/recent-work/`/`docs/agent-memory/` historical narrative (which correctly stays unedited per Rule 1/Rule 2). `RP1`'s cross-reference to "`M4`" (`compliance-checklist.md:215`) and `README.md:29`'s "`R1`" both correctly resolve to the *other*, un-renamed `M4`/`R1` (Section 11 multi-agent, Section 9 reviewer-turn gate respectively) — not stale references to the old feature-map/resume IDs.
