# Review — P103 [Stop hook fires only on code projects — drift half joins the ship gate]

**Date:** 2026-09-05
**Agent:** solo
**Reviewer agent:** security-reviewer, code-reviewer, silent-failure-hunter, pr-test-analyzer, compliance-reviewer, diagram-builder (the six agents `ship-sop.config.json` enables; one run for this Step 1b turn and the ship gate)
**Files changed:** 9
**Lines changed:** [+186 / -12] against main
**Commits:** 4f7f153 (feature), 650bb14 (review fixes)

Review mechanics: six agents read-only against a detached worktree at 4f7f153; findings returned inline; artifacts written by the session. Each fix carries a fixture that fails against 4f7f153.

## Summary

`sop-stop-drift.sh` exits 0 unless `sop_is_code_repo`, so a prose project with SOP scaffolding gets no Stop notice at all; the context block's closing line says whether anything is enforced. Fixes from review: the library runs in the C locale (one invalid byte in CLAUDE.md blanked the whole parse under BSD grep); a contradicted `non-code` declaration and a dangling CLAUDE.md symlink are named in a `Project type:` line of every context block, config or not, naming both consequences; in-flight edits leave the Stop throttle signature (the notice's own "carry on" instruction re-fired it on the first live run); the record helper creates its directory. 85 fixture cases.

Severity: NONE remaining (one CRITICAL, one HIGH, two MEDIUM, two LOW found; all fixed in 650bb14)

## Findings

- [silent-failure-hunter, CRITICAL] `sop-lib.sh`: BSD grep in a UTF-8 locale aborts the file on one invalid byte; with stderr discarded that read as no declaration and no signals, so a pasted smart quote in CLAUDE.md turned a code project non-code and silenced the Stop hook, the ship gate and the push gate together. Fixed: `export LC_ALL=C` in the library (all patterns are ASCII). Fixtures `type-invalid-byte-does-not-blank-declaration`, `-heuristics`.
- [silent-failure-hunter, HIGH] the declared-non-code contradiction was only named inside the `Ship gate:` line, which prints only where a ship-sop config exists — and the Stop hook's silence now rides on the same answer. Fixed: a `Project type:` line prints in every block when a declaration contradicts the signals. Fixture `ctx-contradiction-named-without-config`.
- [silent-failure-hunter, MEDIUM] a CLAUDE.md symlink whose target is gone read as missing and fell through silently. Fixed: `sop_claude_md_state`; the block names it. Fixture `ctx-dangling-claude-md-named`. Kept fail-open to non-code rather than defaulting to code.
- [security-reviewer, MEDIUM] the contradiction text named only the reviewer gate. Fixed: names both consequences. [LOW] compounding of the fail-open classifier across two controls noted in the hook header.
- [code-reviewer, LOW] an unasserted hook call in the new block; now `stop-non-code-initial-silent`. Also a pre-existing stray error: the record helper wrote into a directory git drops on branch switch; it now creates it.
- [pr-test-analyzer, NEEDS-TESTS] the declaration was never exercised against the drift half in either direction. Fixed: `stop-declared-code-without-manifest-fires`, `stop-declared-non-code-all-facts-silent-no-marker` (all three facts, no throttle marker written).
- [live, this session] the in-flight edit the notice prescribes re-fired the notice as a new dirty set; in-flight paths now leave the throttle signature (`stop-inflight-edit-does-not-refire`, with its two neighbours proving other edits still re-fire and the file is still listed).
- [compliance-reviewer] APPROVE, no findings; README claim checked against the exact guard line.
- [diagram-builder] Stop-hook guard-order flowchart added to `docs/diagrams/p102-project-type-and-ship-gate.md`.

## Notes

Agent-id during this session read `1e7aa9` while the review worktree existed (two worktrees → path hash per the multi-agent rule) and `solo` once it was removed. Records here use `solo`.
