# Review — P102 [ship-sop gates fire only on code projects; one project-type rule; SOP commands stop outside SOP repos]

**Date:** 2026-09-04
**Agent:** solo
**Reviewer agent:** security-reviewer, code-reviewer, silent-failure-hunter, pr-test-analyzer, compliance-reviewer, diagram-builder (the six agents `ship-sop.config.json` enables; one run served both this Step 1b turn and the ship gate)
**Files changed:** 21
**Lines changed:** [+486 / -63] (feature 0324597 plus fixes 90075fc, 8903290, against main)
**Commits:** ffb4380 (files P102 as in progress), 0324597 (feature), 90075fc (review fixes), 8903290 (silent-failure fixes)

Review mechanics: all six agents ran read-only against a detached worktree at 0324597 (per the 2026-09-04 gotcha; the live tree was never their cwd), returned findings inline, and this artifact plus the gate report were written by the session. Every finding below was reproduced or checked before the fix, and each fix carries a fixture that fails against the commit it fixes.

## Summary

Adds `sop_project_type` / `sop_is_code_repo` / `sop_declared_project_type` / `sop_code_signals` to `scripts/hooks/sop-lib.sh` and the CLI wrapper `scripts/hooks/sop-project-type.sh`; `sop_shipsop_gate` returns nothing for a non-code project and always counts code lines only; `sop_shipsop_covered` uses the same count; `sop-session-context.sh` names the type in its header and explains an absent gate; `install-hooks.sh` installs the wrapper; `/update-sop` and `/restart-sop` open with an is-this-an-SOP-project gate calling `sop_is_sop_repo`; `/update-sop` Step 2, `/finish`, `sop-checker` and the compliance checklist (new X7) read the shared rule; both CLAUDE.md templates declare their type; agent-sop declares `code`; 31 new fixture cases (71 total); one Mermaid diagram from the gate run.

Severity: NONE remaining (one CRITICAL, one HIGH, one HIGH-recommended, four MEDIUM and three LOW were found and fixed in 90075fc and 8903290)

## Findings

- [security-reviewer, HIGH] `sop-lib.sh` `sop_project_type`: a `**Project type:** non-code` line silently overrides a manifest or an `## Auth`/`## Database` heading and turns the gate off with no signal. Fixed: `sop_code_signals` lists the hits and the context block's `Ship gate:` line names the contradiction (fixture `ctx-declared-non-code-over-manifest-named`, fails against 0324597); checklist X7 audits it.
- [security-reviewer, LOW] `sop_shipsop_gate`: config-supplied branch pattern passed to grep without `--`. Fixed: `grep -Eq -- "$pat"`.
- [code-reviewer, MEDIUM] declaration read inside fenced code blocks. Fixed: `sop_claude_prose` strips fences before the declaration and heading checks (fixture `type-fenced-example-is-not-a-declaration`, fails against 0324597).
- [code-reviewer, MEDIUM] `/update-sop` and `/restart-sop` re-implemented `sop_is_sop_repo` inline, dropping the HOME guard. Fixed: the gate block sources the installed `sop-lib.sh` and calls the function; the inline test remains only for a `--no-hooks` machine.
- [code-reviewer, MEDIUM] no audit of a declaration contradicting the heuristics. Fixed: X7 in `compliance-checklist.md` (Recommended; totals 86/95), `sop-checker.md` reports it.
- [code-reviewer, LOW] a bulleted declaration (`- **Project type:** code`) was ignored. Fixed: leading bullet stripped (fixture `type-bulleted-declaration-counts`).
- [silent-failure-hunter, CRITICAL] `sop_code_lines` split numstat on whitespace, so a rename `docs/plan.md => src/gen.js` was classified by the old name and its code lines counted as documentation — with the docs filter now unconditional, a covered branch could stay covered while unreviewed code arrived. Pre-existing on main. Fixed in 8903290: tab-separated fields, rename excluded only when both names are documentation (fixture `stop-rename-doc-to-code-counts`, fails against 90075fc).
- [silent-failure-hunter, HIGH] three heuristics case-sensitive while the declaration is not. Fixed: `-i` throughout, awk `tolower` on the Key Commands heading (two fixtures, fail against 90075fc).
- [silent-failure-hunter, MEDIUM] `code-focused` after the keyword failed to parse. Fixed: trailing class `[^a-z]` (fixture `type-declaration-followed-by-hyphen`).
- [pr-test-analyzer, NEEDS-TESTS] `sop_shipsop_covered`'s dropped parameter had no case reaching its only distinguishing branch; the real templates were never fed through the rule; a subdirectory-only manifest, CRLF, last-section Key Commands, the reason text and an installer upgrade were unpinned; my comment "Fails pre-P102" on the `skip_docs_only: false` case was false (the old jq `// true` default already swallowed an explicit `false`). Fixed: seven fixtures added, comment corrected.
- [compliance-reviewer] APPROVE, no findings requiring action; PII/GDPR/HIPAA/retention not applicable (developer tooling, no personal data; the pre-existing bypass-log surface is unchanged by this diff).
- [diagram-builder] one flowchart of the rule and the gate path, `docs/diagrams/p102-project-type-and-ship-gate.md`; no API catalog (no HTTP surface).

## Notes

Out of scope, recorded for the operator: the drift half of the Stop hook (session record, tracker hygiene) still applies to every SOP project regardless of type — Meaningful, an archive with SOP scaffolding and one unrecorded commit, gets that notice. Only the reviewer gates are code-only. Also outside agent-sop: 30 ECC user-scope hooks fire in every repository on this machine, gated by ECC's own profile flags, not by anything here.
