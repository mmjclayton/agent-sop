Covers: b8e6874bd8e6a5f0237ca934fc94279ab82ee2eb

# Codex publication gate — agent-sop

## Summary

PASS with non-blocking follow-ups. No unresolved HIGH or CRITICAL findings.
Severity: MEDIUM

P107 and P109: native Codex installation, skills, isolated reviewers, shared hook policy,
explicit enforcement port and a simpler public README. Full review range:
`8a07a0a2e298e0815eec3551d4d8e11f57cff755..b8e6874bd8e6a5f0237ca934fc94279ab82ee2eb`.

Coverage is cumulative: all configured reviewers inspected the full port through
`76b3bb6`, then reviewed fixes in `76b3bb6..2e13c61` and `2e13c61..b8e6874bd8e6a5f0237ca934fc94279ab82ee2eb`.
Historical blocking findings below were corrected in those independently reviewed
deltas. This report does not relabel an earlier review as covering a later commit.

| Reviewer | Blocking threshold | Final fix review | Outstanding blocking findings |
|---|---|---|---|
| code-reviewer | HIGH | PASS | 0 |
| security-reviewer | CRITICAL | PASS | 0 |
| silent-failure-hunter | HIGH | PASS | 0 |

## Findings and dispositions

- Fixed replica validation bypasses in `scripts/validate-state-transitions.sh`:
  structural JSON parsing handles inline empty exclusions; Codex uses user-owned
  baselines; project exclusions cannot suppress native asset checks; missing user
  configuration fails when project replication configuration exists. Stale-mirror,
  project-shadowing and missing-configuration fixtures verify these paths.
- Fixed `setup.sh` and `scripts/setup-codex.sh` registration checks: stale hook
  paths cannot be accepted as working auto-mode or retire the legacy handler.
- Fixed earlier review findings: trusted user-installed reviewer executable and
  policy; disabled external reviewer integrations; source-link and customization
  preservation; safe priority marker validation; checked asset discovery; native
  instruction failures remain visible and conservatively retain code enforcement.
- Enforcement changes are explicitly declared under agent-sop P109 / ship-sop P30.
  Independent source reviews and the established pre-port validator provide
  evidence beyond results from the modified enforcement layer itself.
- MEDIUM follow-ups remain in agent-sop P108: sync/install ownership bookkeeping,
  shared instruction symlinks and the documented legacy migration limitation.
  ship-sop P29 tracks the symlink replica fix; P31 tracks empty ownership records
  preserving artifacts after Codex-then-Claude uninstall. None reaches its
  configured blocking threshold. No claim is made that these are fixed.

## Validation

- All eight agent-sop fixture suites pass, including 30 state-transition cases.
- ship-sop installation/isolation fixtures pass, including hostile project runner,
  conflicting verdicts, forced dual-runtime config preservation and stale hooks.
- Shell syntax, relevant ShellCheck checks, configuration/frontmatter checks and
  README local links pass. Existing customized global reviewer profiles remain.
- Pre-port validator passed the final Backlog transitions; its original 29 state
  fixtures passed. It incorrectly skipped the new inline-exclusion stale-mirror
  case; the fixed validator rejects that case as expected.
- Real native replication checks pass after synchronization (20 changed tracked
  files upstream). The parent ran suites; read-only reviewers performed source
  analysis in independent clones, using the trusted installed runner.

## Live runtime feedback

User-provided live evidence confirmed project-root hook payloads, exec_command
normalization to Bash/command, and production Stop continuation with the expected
tracker-drift notice. SessionStart, UserPromptSubmit and Stop were also observed
in a disposable runtime. A raw exec_command fixture was not evidence of a
translation bug. A complete automatic review-to-push cycle is not claimed by
those lifecycle tests. No merge or release is authorized by this gate report.

## Independent review evidence

The following are verbatim source-review results. Earlier BLOCK verdicts are
historical and their findings are resolved by the subsequent reviewed fixes above.

### Full port: code-reviewer

## Review Summary

| Severity | Count | Status |
|----------|-------|--------|
| CRITICAL | 0 | pass |
| HIGH | 1 | warn |
| MEDIUM | 0 | pass |
| LOW | 0 | pass |

## Findings

[HIGH] Project configuration silently disables Codex replication checks  
File: `scripts/validate-state-transitions.sh:378`  
Evidence: The validator selects project `.codex/agent-sop.config.json` before user configuration. However, `scripts/sync-sop-files.sh:46` deliberately stores Codex baselines in user configuration and reads project configuration only for exclusions. A valid project configuration containing `{"exclude":[]}` therefore makes the validator report empty baselines and exit successfully at line 411, even when installed replicas are stale.  
Fix: Match the updater’s Codex configuration precedence: read baselines from user configuration unless explicitly overridden, and merge project exclusions separately. Add a fixture with project exclusions, populated user baselines and a stale installed replica.

Source analysis completed. No files written or test suites run; test execution remains with the parent.

Verdict: BLOCK

### Full port: security-reviewer

Source analysis completed. One security finding:

- **HIGH - scripts/validate-state-transitions.sh:378 - Project configuration bypasses Codex replication checks.** The validator selects project configuration before user configuration, while `sync-sop-files.sh:47` deliberately keeps Codex ownership hashes in user configuration. A project file containing `{"exclude":[]}` therefore shadows installed baselines, produces an empty manifest and exits successfully at lines 409-411. Changed reviewer policies can remain unreplicated without triggering the gate.
  **Fix:** Match the synchroniser’s configuration precedence: obtain Codex baselines from user configuration and apply project exclusions only to project-scoped assets.
  **Verification:** Add a fixture with populated user baselines, a stale installed reviewer and project configuration containing only exclusions. Require a blocking result.

No hardcoded secrets found. No Node dependency manifest found, so `npm audit` was inapplicable. Test execution remains with the parent; no suites or file writes were performed.

Verdict: BLOCK

### Full port: silent-failure-hunter

- **HIGH - `scripts/validate-state-transitions.sh:378` - Project config silently disables Codex replication checks.**  
  **Evidence:** The validator selects `.codex/agent-sop.config.json` before the user config. However, `scripts/sync-sop-files.sh:46` deliberately stores Codex baselines in the user config and treats project configuration as exclusions only. A valid project config such as `{"exclude":[]}` therefore reaches the “baseline_shas is empty” branch at line 409 and exits successfully.  
  **Impact:** Changed native skills and reviewers can remain stale at user scope without the replication gate detecting them.  
  **Suggested fix:** Match the sync script’s configuration resolution: obtain Codex baselines from the user config, apply project exclusions separately, and retain explicit config overrides. Add a regression case with project-only exclusions and a stale installed mirror.

Source analysis completed. No suites run or artifacts written.

Verdict: BLOCK

### First fix delta: code-reviewer

## Review Summary

| Severity | Count | Status |
|----------|-------|--------|
| CRITICAL | 0 | pass |
| HIGH | 0 | pass |
| MEDIUM | 0 | pass |
| LOW | 0 | pass |

## Findings

No issues - `scripts/validate-state-transitions.sh:378` selects user-owned Codex baselines, and `:479` prevents project exclusions from suppressing native asset checks. The regression fixture at `docs/benchmark/codex-fixtures/run-tests.sh:143` checks the baseline-shadowing failure.

The validator change falls within P109’s declared enforcement scope. Source analysis completed; test execution remains with the parent. No artifacts written.

Verdict: PASS

### First fix delta: security-reviewer

Severity: NONE

No security findings in the requested diff. `scripts/validate-state-transitions.sh:378` selects user-owned Codex baselines; lines 421-424 validate project exclusions, and lines 478-481 prevent those exclusions from bypassing replica checks for native agent assets. Added values remain quoted and are processed as data.

Source analysis and Bash syntax checks completed. No secrets or dependency changes were introduced. Fixture execution remains with the parent, as requested.

Verdict: PASS

### First fix delta: silent-failure-hunter

No silent-failure findings in the requested diff. Reviewed configuration selection, exclusion handling, error propagation and the regression fixture.

Source analysis complete. Tests remain with the parent as requested.

Verdict: PASS

### Final configuration fix: code-reviewer

## Review Summary

| Severity | Count | Status |
|----------|-------|--------|
| CRITICAL | 0 | pass |
| HIGH | 0 | pass |
| MEDIUM | 0 | pass |
| LOW | 0 | pass |

## Findings

No issues - `scripts/validate-state-transitions.sh:385` correctly blocks missing user configuration when project Codex configuration exists. The regression fixture at `docs/benchmark/codex-fixtures/run-tests.sh:157` checks both failure status and the specific diagnostic.

The validator change falls within P109’s declared enforcement scope. Source analysis completed for the requested range; test execution remains with the parent.

Verdict: PASS

### Final configuration fix: security-reviewer

Source analysis completed for the specified diff. No security findings.

The change fails closed when project Codex configuration exists but user configuration is missing. Added fixture operations use quoted paths within the temporary workspace. No secrets or dependency changes were introduced.

Test execution remains with the parent, as requested.

Verdict: PASS

### Final configuration fix: silent-failure-hunter

No findings in the scoped diff.

The guard at `scripts/validate-state-transitions.sh:385` reports missing user configuration to stderr and exits non-zero when project Codex configuration exists. The fixture at `docs/benchmark/codex-fixtures/run-tests.sh:157` checks both failure status and the specific diagnostic.

Source analysis completed. Test execution remains with the parent as requested.

Verdict: PASS
