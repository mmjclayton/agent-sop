# Review — P104 [Hook fixes from the token review]

**Date:** 2026-09-05
**Agent:** solo
**Reviewer agent:** security-reviewer, silent-failure-hunter, code-reviewer (the three gates the config now enables, launched with `isolation: "worktree"`)
**Files changed:** 9
**Lines changed:** [+205 / -38] against main
**Commits:** 3d528a4 (feature), ea2fdb0 (review fixes)

## Summary

Gate demand forbids Backlog filing and names the isolation flag; the context block prints only non-default facts; a legacy resume marked superseded is never served; three default gates in this repo's config; the push matcher ignores here-document bodies (found live when the gate refused a scratchpad write). 92 hook cases, 20 resolver cases.

Severity: NONE remaining (one CRITICAL, one HIGH, four MEDIUM, two LOW found; all fixed in ea2fdb0)

## Findings

- [silent-failure-hunter, CRITICAL] `resolve-resume-path.sh`: the superseded check was an unanchored substring on line 1, so prose containing "superseded" dropped a real resume. Fixed: anchored to `**SUPERSEDED`; fixture `read-serves-legacy-mentioning-superseded-in-prose`.
- [silent-failure-hunter, HIGH] the marker after a leading blank line was served. Fixed: first non-blank line, BOM and CR stripped; fixtures for blank line, CRLF, BOM.
- [silent-failure-hunter, MEDIUM] no stderr when skipping; fixed. [MEDIUM] no resolver-suite coverage; five cases added. [LOW] tautological closing-line assertion; now literal. [LOW] MEDIUM/LOW routing unstated in the demand; stated.
- [security-reviewer, MEDIUM] same anchoring point plus diagnosability; fixed as above. APPROVE otherwise; the conditional printing was verified to leave every actionable line unconditional.
- [code-reviewer, MEDIUM ×3, LOW] README and harness guide described unconditional printing; diagram doc quoted a deleted sentence; no stale-sync fixture; Backlog wording overstated. All fixed.
- [live] the push gate refused a scratchpad write whose heredoc mentioned a push verb. Fixed: `sop_strip_heredocs`; two fixtures (a verb inside a body is silent, a push after the body is still refused). The first cut of those fixtures passed vacuously through an invalid JSON escape; rewritten without quote characters.
