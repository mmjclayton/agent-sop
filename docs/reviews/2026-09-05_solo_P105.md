# Review — P105 [Prose trim, validator retarget, Backlog archive]

**Date:** 2026-09-05
**Agent:** solo
**Reviewer agent:** security-reviewer, silent-failure-hunter, code-reviewer (the three default gates, launched with `isolation: "worktree"`)
**Files changed:** 45
**Lines changed:** [+2,180 / -3,850] against main (estimate from the two commits' stats)
**Commits:** 59b4ae5 (trim, retarget, archive), 465dbdf (review fixes), 1bff70d (post-gate fix, below)

## Summary

Core SOP 791 → ~110 lines, `/update-sop` 669 → ~115, `/restart-sop` 323 → ~40, `/finish` 200 → ~30, templates and this CLAUDE.md follow; the review citation moves from a Batch Log line to the Backlog entry; closed items older than 90 days archive to `docs/backlog-archive.md`; checklist trimmed to what is still read. Three gates, all BLOCK at 59b4ae5, all findings fixed in 465dbdf with fixtures that fail against 59b4ae5.

Severity: NONE remaining (found: 5 CRITICAL, 9 HIGH, 6 MEDIUM, 3 LOW)

## Findings

- [security, CRITICAL] citation regex allowed `docs/reviews/../../<real file>` to clear the gate (pre-existing shape, now on the surface every session edits). Fixed: the citation is a labelled `review:` line naming a bare filename under `docs/reviews/`; fixture `illegal-review-citation-traversal`.
- [security, HIGH] citation matched anywhere in the entry, code fences included. Fixed: labelled lines outside fences only; fixtures `illegal-review-citation-in-fence`, `-in-prose`.
- [silent-failure, CRITICAL] entry extraction ran past `## Shipped Archive` to end of file, lending the last entry a foreign citation, and under trigger (b) too. Fixed: bounded at the next `### ` or `## `; fixture `illegal-feature-citation-in-following-section`, `-in-next-entry`.
- [silent-failure, CRITICAL] `### P313: title` and a bare CRLF heading gave an empty body and a silent pass. Fixed: anchor `### P<n>` followed by a non-digit, CR stripped, and a BLOCK when the heading cannot be located; fixtures `illegal-feature-colon-heading-no-review`, `-crlf-heading-no-review`.
- [silent-failure, HIGH] skip token in prose accepted. Fixed with the labelled-line rule.
- [silent-failure, CRITICAL ×2; security HIGH ×2] `archive-backlog.sh`: a status tag quoted in an active entry's body moved the entry; a `### P` inside a code fence split an entry; Backlog.md was written before the archive; a `## ` line inside a non-last entry was split off. Fixed: the entry's own first bracket line is its status; fences masked (length-preserving) before splitting; archive written first, both through temp file and atomic replace; only the last entry is scanned for a trailing section or footer; `[WON'T]` archives only when its status or Reason line carries a date; hyphen headings tolerated. Suite `docs/benchmark/archive-fixtures/run-tests.sh`, 13 cases.
- [security, HIGH] `security.md` rule 11 still said "Batch Log line" and "Step 3c/3d". Fixed.
- [code, HIGH ×5] priorities block stale after the retags (refreshed); `setup.sh` and the build-plan template still scaffolded feature-map (removed); the checker cited Steps 1b/3c, kept Q1/Q2 guidance, grepped a deleted string for S4 and disagreed with H1 (rewritten); the no-hooks `/restart-sop` path had lost the tamper check (restored: `git status --porcelain` on the context files); checklist summary arithmetic and section numbers (regenerated from the sections: 79 code / 73 non-code).
- [code, MEDIUM ×3] stale step numbers in validator messages, resolver, rollup, tracker scripts, README, multi-agent doc, review template, config template (fixed); `archive-backlog.sh` had no caller (now `/update-sop` Step 3); dead conditional (gone).

## Notes

After the gate run, replaying the validator against main with `--before main` on the real Backlog exited 141 with no output: the new entry extraction piped the whole file through `tr` into an awk that exited at the next heading, so on a file larger than the pipe buffer `tr` took SIGPIPE and errexit ended the script silently, the P73 shape. Fixed in 1bff70d (CR stripped once to a temp file; no early exits), with a generated large-file fixture that fails against 465dbdf. That 30-line fix was not re-reviewed by the agents; it is covered by the fixture suite and pushed with the logged bypass.


Three gate runs on this branch cost 152,782 + 143,602 + 258,354 tokens (harness counters). They found five CRITICALs. The old six-agent set would have added compliance and diagram runs that found nothing on this repo twice before.
