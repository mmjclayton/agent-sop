# Review — P60-P63 + P65 (Digest-review corrections batch)

**Date:** 2026-07-06
**Agent:** solo
**Reviewer agent:** code-reviewer
**Files changed:** 12 (at review time; 20 after session-end housekeeping)
**Lines changed:** +194 / -17 (implementation commits, pre-housekeeping)
**Commits:** 9d5d36f, 8ac905a, ed7f495, 492ede9, b70d842, c8b19ed

## Summary

Implements the 2026-07-06 digest-review slate: tokenizer-relative token figures + `/usage` measurement + external citations (P60); memory-poisoning guard in `security.md` rule 1 and restart-sop Step 4 + S4 check (P61); background-subagent pre-flight check in `/update-sop` + M6 check (P62, `[Bug]`); CI hardening rule 10 + deny-rule examples + S5/S6 checks (P63); `/simplify` version note + README count refresh (P65). Check totals 78/87 → 82/91. SOP-self-modification trigger fired this review unconditionally.

Severity: HIGH (2 HIGH, 3 MEDIUM, 2 LOW — all resolved before merge, see Findings)

## Findings

All findings below were reported by the reviewer turn and resolved in the session-end commit; each entry records the resolution.

- [HIGH] Session-end housekeeping (Step 10) uncommitted at review time — `Backlog.md`, `CLAUDE.md`, `docs/agent-memory.md`, `docs/feature-map.md`, `docs/build-plans/phase-0-foundation.md` modified but not committed. **Resolved:** deliberate sequencing (review ran before Step 10); housekeeping committed immediately after this artifact was written, same branch, before PR.
- [HIGH] `docs/sop/compliance-checklist.md:337-338` Maximum-deductions formula still used pre-session counts (15/46/17 → 414, 15/55/17 → 459) while the Check Summary table showed 16/48/18. **Resolved:** formulas corrected to `16 x 10 + 48 x 5 + 18 x 2 = 436` (non-code) and `16 x 10 + 57 x 5 + 18 x 2 = 481` (code).
- [MEDIUM] Duplicate `## Step 0` heading in `.claude/commands/update-sop.md:8` — new pre-check reused the `Step 0` label already held by `resolve_agent_id`, making the M1 pointer ambiguous. **Resolved:** renamed to `## Pre-flight check: No outstanding background subagents`; M6 wording in `compliance-checklist.md`, `sop-checker.md`, and `multi-agent.md` updated to match; narrative docs updated.
- [MEDIUM] `Backlog.md` P65 acceptance criteria cited pre-session totals (78/87) as the verification target though P61-P63 had moved them to 82/91. **Resolved:** criteria line updated to 82/91.
- [MEDIUM] `.claude/agents/sop-checker.md` had no `S3` block (pre-existing gap on `main`) and its Setup line still said "S1/S2 checks". **Resolved:** S3 block added after S2; Setup line now reads S1-S6.
- [LOW] `Backlog.md` P63 description named an `Agent(model:...)` deny example the shipped JSONC block lacked. **Resolved:** `"Agent(model:opus)"` example line added to the `security.md` rule 7 block, matching the description and the original digest intent.
- [LOW] Version stamps (`sop_version` / `SOP-Version`) not bumped on five substantively edited files. **Resolved:** stamps bumped to 2026-07-06 on `compliance-checklist.md`, `multi-agent.md`, `sop-checker.md`, `restart-sop.md`, `update-sop.md`, `finish.md` (advisory only per `/update-agent-sop` — SHA comparison remains the authority).

## Notes

Verified clean by the reviewer: restart-sop Step 4 bash guard tested against this repo and a scratch repo (exit 0, surfaces dirty/untracked context files, no error on missing paths); JSONC deny block matches Claude Code's `permissions.deny` shape; cross-file check-ID references (M1-M6, S1-S6, totals 82/91 in `README.md:29`, `feature-map.md`, batch log) mutually consistent; Check Summary arithmetic recounted row-by-row and reconciles to 16/48/18/82 and 16/57/18/91; no Rule 1 violations.

Out of scope (pre-existing on `main`, noted for a future item): `compliance-checklist.md` reuses `M1`-`M4` IDs across two unrelated check families (section 4 feature-map structure vs section 11 multi-agent). Filed as an observation only — renumbering is a breaking change for consumers' audit records.
