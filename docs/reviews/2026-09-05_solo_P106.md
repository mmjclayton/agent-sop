# Review — P106 [/update-agent-sop runs scripts/sync-sop-files.sh]

**Date:** 2026-09-05
**Agent:** solo
**Reviewer agent:** security-reviewer, silent-failure-hunter, code-reviewer (default gates, `isolation: "worktree"`)
**Files changed:** 6
**Lines changed:** [+330 / -70] against main (estimate from the two commits)
**Commits:** 519db25 (feature), 75688b4 (review fixes)

## Summary

`scripts/sync-sop-files.sh` replaces the prose three-way of `/update-agent-sop`: a consumer file equal to the baseline or any past upstream version (git history, renames followed) is pristine and updated; a local edit is kept or reconciled; the config's baselines and last-check date are rewritten only on `--apply`. All three gates BLOCK at 519db25; every finding fixed in 75688b4 with fixtures that fail against 519db25 (25 cases; 10 discriminate).

Severity: NONE remaining (found: 4 CRITICAL, 6 HIGH, 6 MEDIUM, 4 LOW)

## Findings

- [security, CRITICAL ×2] a manifest destination was concatenated onto the root with no containment, so `..` escaped the consumer and an absolute user-scope path wrote anywhere. Fixed: `under()` resolves the longest existing ancestor physically and refuses a `..` in the tail, for destinations against the consumer root or `~/.claude/` and for upstream paths against the checkout; refused rows are reported and never written. [code, CRITICAL] the first containment cut ran `mkdir -p` before the check, creating the parent directory even in a dry run; the check now needs nothing created. Fixtures `traversal-destination-refused`, `absolute-user-destination-refused-and-no-directory-created`, `upstream-path-escape-refused`.
- [silent-failure, CRITICAL ×2] a row with an unrecognised scope vanished from the report, and a manifest that failed to parse read as all clear. Fixed: every two-path row is counted and the run fails naming any row not classified; zero rows is an error. Fixtures `unknown-scope-row-is-an-error-naming-the-row`, `unparsable-manifest-is-an-error`.
- [security/silent-failure, HIGH] `[ a = b ] || jq ... && mv` grouped as `(a || jq) && mv`, so `mv` ran on every in-sync file, dry runs included, with spurious errors; fixed with an `if`. [HIGH] the backup `cp` was silenced and the replace unconditional; now checked, JSON validated, temp file beside the config, atomic rename. [HIGH] `mv` severed a symlinked config; written through the link. Fixtures `apply-run-writes-nothing-to-stderr`, `config-and-backup-are-valid-json`, `symlinked-config-written-through`.
- [silent-failure, MEDIUM] `git log` without `--follow` missed renamed files (finish.md was go.md); the first `--follow` cut still showed the current name at pre-rename revisions. Fixed: hash and name pairs from `--follow --name-only`; a shallow upstream is warned about. Fixture `renamed-upstream-file-found-in-history`. [MEDIUM] an unreadable consumer file was reported as a local edit; now `UNREADABLE`. [MEDIUM, code] a first-run local customisation with no baseline printed as plain modified; now `RECONCILE`. Fixture `first-run-customisation-is-reconcile`.
- [code, HIGH ×2] README still described the three-way; `REFUSED` and `absent-upstream` were undocumented in the command. [MEDIUM] the Step 1 snippet ignored a project-scope config and swallowed a failed pull. [LOW ×3] exclusion checked after existence; the script's non-row status unstated; no stderr assertion. All fixed.
- [security, HIGH, design] copying into `~/.claude/` from a checkout is a supply-chain surface; the command now states that `local_path` is the trust root and that a project config carrying it must not be committed.
