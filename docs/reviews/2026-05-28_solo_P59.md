# Review — P59 Step 1b reviewer-gate tightening + cross-layer rules guide

**Date:** 2026-05-28
**Agent:** solo
**Reviewer agent:** code-reviewer
**Files changed:** 5 (agent-sop) + 1 (ship-sop)
**Lines changed:** +200 / -2 (agent-sop), +12 / -0 (ship-sop)
**Commits:** (pending — single squash commit on `feat/reviewer-tightening-and-cross-layer-guide`)

## Summary

Two upstream SOP tightenings driven by hst-tracker 2026-05-28 evidence. (1) Step 1b reviewer-gate in `docs/sop/claude-agent-sop.md:407-421` gains explicit triggers (size / SOP-self-mod / project-declared) + skip list (docs-only / test-only / dep bumps) + `review_loc_threshold: 0` always-on-code semantics + rationale paragraph at `docs/sop/claude-agent-sop.md:430`. (2) New `docs/guides/cross-layer-rules.md` (165 lines) codifies the unify-first / parity-fixture pattern with inventory as the load-bearing artifact. Cross-refs added to `CLAUDE.md:34-35` and `docs/guides/sop-common-mistakes.md:23`. Backlog entry P59 in `Backlog.md:1113-1129`. ship-sop side: 12-line clarification paragraph in `ship-sop/README.md:30-39` describing the per-stop-vs-per-session reviewer-gate relationship — no mechanics change.

Severity: LOW

## Findings

- [`docs/sop/claude-agent-sop.md:413`] ~~The new trigger (b) names `docs/sop/**`, `docs/guides/sop-*.md`, `.claude/agents/**`, `.claude/commands/**`, `scripts/validate-*.sh` as SOP self-modification paths. The list is project-specific to agent-sop's own layout; consumers without `scripts/validate-*.sh` will read this as a path their project must also have.~~ **ADDRESSED in this PR** — `docs/sop/claude-agent-sop.md:413` rephrased: "any edit to files that the SOP itself executes or instructs (SOP docs, reference agent definitions, slash commands, validators that gate other steps). Examples in agent-sop's own layout: [...]. Projects without one of these paths simply have nothing matching — the trigger doesn't fire."

- [`docs/sop/claude-agent-sop.md:414`] ~~Trigger (c) references `agent-sop.config.json#review_triggers[]` as a per-project config option, but `docs/templates/agent-sop-config-template.json` doesn't yet define this field.~~ **ADDRESSED in this PR** — `docs/templates/agent-sop-config-template.json:26-27` adds `review_triggers: []` with explanatory note describing array-of-globs semantics.

- [`docs/sop/claude-agent-sop.md:416`] Skip-list extension `.py / .go / .rs` etc. is fine but `docs/sop/claude-agent-sop.md:417` skip definition for test-only commits uses glob fragments `*_test.*`, `*.test.*`, `*.spec.*`, `__tests__/` — these cover most ecosystems but miss Rust's `tests/` directory convention and Java's `src/test/`. Not load-bearing for v1; flag as a known gap to revisit if/when a Rust or Java project hits the gate. No fix required.

- [`docs/guides/cross-layer-rules.md:50-65`] The inventory template is presented as a copy-paste markdown block but doesn't say *where* it lives. The `docs/process-improvements.md` reference in the intro (`docs/guides/cross-layer-rules.md:44`) is implicit. Suggest naming the canonical home explicitly: "Add this table to a living document — recommended location is `docs/process-improvements.md`, but `docs/agent-memory.md` Gotchas or a dedicated `docs/duplicated-logic-inventory.md` are equivalent. Pick one and stick with it." Reader is otherwise left to guess.

- [`docs/guides/cross-layer-rules.md:113-126`] The worked example uses pseudocode (`load fixture ... for each entry: assert ...`) rather than JavaScript or any specific language. This is the right call for a project-agnostic guide, but the pseudocode loop is terse enough that a reader from a non-JS background might miss that the fixture loads as a list-of-objects. Suggest adding one line above the pseudocode: "// fixture is JSON array of {input, expected} objects." Minor — readers will infer it but it removes one ambiguity.

- [`ship-sop/README.md:33-39`] ~~The clarification paragraph asserts "Findings dedupe naturally because both gates read the same diff and produce concrete file:line anchors." This is a hopeful claim, not a measured one.~~ **ADDRESSED in this PR** — `ship-sop/README.md:39` softened to acknowledge the gates run at different times and the overlap is not measured, while explaining why a reader can still cross-reference the two artifacts.

## Notes

- This is a docs-only PR (no code change). Under the new Step 1b rules introduced by this very PR, trigger (b) — SOP self-modification — fires, which is why this review artifact exists. Self-applying the new policy on its own introducing-PR is the right dogfood.
- Skip list coverage for Rust `tests/` and Java `src/test/` (Finding 3) is a known follow-up. Filed as gap, not blocker.
- ship-sop's clarification paragraph could equally live in `ship-sop/CLAUDE.md` Common Mistakes or `ship-sop/docs/ship-sop.md`. Chose README because that's the public-facing spec where the gate table introduces `@code-reviewer`; the question "how does this relate to agent-sop?" arises at exactly that point.
- Empirical justification (2026-05-28 composer fix — 220 LOC, two HIGH bugs caught at review) is captured in `docs/sop/claude-agent-sop.md:430` rationale paragraph. No claim is made that "every PR" mode would have caught it — the existing 50 LOC threshold already would have, and Finding 1 makes that explicit. The change is about giving projects an evidence-driven path to *lower* the threshold, not asserting the threshold was wrong upstream.
