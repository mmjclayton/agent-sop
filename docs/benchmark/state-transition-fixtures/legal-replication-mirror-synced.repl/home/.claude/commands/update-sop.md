# /update-sop

## Step 1b: Required reviewer turn

Declare the skip on the Batch Log line using the enumerated token
`review skipped (P<n>): <docs-only|test-only|dep-bump|below-threshold>`.
A free-text reason does not count.
