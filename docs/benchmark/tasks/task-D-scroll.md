# Task D: Scroll Padding (fast)
## Prompt
> Users can't reach the "Add Exercise" button at the bottom of a workout because it's hidden behind the minimised workout bar. Fix the scroll padding. Run client tests.
