# Phase 0 — Foundation

Status: In Progress

---

## Problem

The Agent SOP exists as loose documents in an outputs folder. It needs a proper home — a structured project that itself follows the SOP, can be shared publicly, and extended with templates and examples.

---

## Scope

| Batch | What | Priority |
|-------|------|----------|
| 0.1 | Project scaffold (all standard files) | P0 |
| 0.2 | Publish core SOP document (P1) | P0 |
| 0.3 | Publish CLAUDE.md template (P2) | P0 |
| 0.4 | Publish remaining templates: agent-memory, Backlog, build-plan (P3-P5) | P1 |
| 0.5 | Publish example guides: new project walkthrough, migration guide (P6-P7) | P1 |

---

## Architecture

Pure markdown library. No code, no build process. Structure:

```
agent-sop/
  CLAUDE.md
  Backlog.md
  README.md
  docs/
    agent-memory.md
    feature-map.md
    sop/
      claude-agent-sop.md
      variants/         (P8-P10, future)
    templates/
      claude-md-template.md
      agent-memory-template.md
      backlog-template.md
      build-plan-template.md
    examples/
      new-project-walkthrough.md
      existing-project-migration.md
    build-plans/
      phase-0-foundation.md
```

---

## Key Decisions Locked In

- [LOCKED] Markdown only — no build tools, no dependencies.
- [LOCKED] Project follows its own SOP — additive-only writes, session checklists, all standard files.
- [LOCKED] Templates in `docs/templates/`. SOPs in `docs/sop/`. Examples in `docs/examples/`.
- [LOCKED] P-numbers in Backlog.md are permanent document identifiers.

---

## Batch Log

*Append-only. Format: YYYY-MM-DD: Batch N.X — description.*

- 2026-08-07: Batch 0.35 — **P96** `[Bug]`: `/update-sop` Step 7, the only step that *writes* the resume file, named `~/.claude/projects/[project-hash]/memory/project_resume_${AGENT_ID}.md` and never resolved `[project-hash]`. Both readers — `/restart-sop` Step 0d and `--check-drift` — derived it from `git rev-parse --show-toplevel`. An agent with no rule writes into the directory the *session* owns, which for a session launched outside the project is the harness catch-all shared across projects. So the snapshot landed where the drift gate never looks (P46 enforcement dead, reported as "no project_resume file found — skipping"), and Step 7's write-side legacy fallback would direct an agent at another project's unsuffixed `project_resume.md`. Reported by an agent on a consumer project that hit Step 7, recognised neither resume file in the shared directory as its own, and refused the step — the SOP instructed the destructive action and only judgement stopped it. Second occurrence: the shared directory's `project_resume_solo.md` is Intelligent Studio's, already marked SUPERSEDED for the same reason. Fixed by unifying the derivation into new `scripts/resolve-resume-path.sh` (Tier A per `cross-layer-rules.md` — pure function of repo root, HOME, agent-id) and rewiring all three callers; the script refuses with exit 2 when the repo root is the home directory, since no project-scoped directory can be derived there. Write-side legacy fallback removed, read-side retained. **Three more instances of the same over-broad-lookup shape found and fixed while in there:** `sop-checker` located memory directories by *substring-matching* the project name against `~/.claude/projects/`, so an audit could score a project against another's files; the SessionStart hook in `harness-configuration.md` read `~/.claude/projects/*/memory/project_resume.md`, every project on the machine, and loaded it as session context; `/restart-sop` Step 2's sibling scan used the same glob and now enumerates `git worktree list` instead, since siblings live in separate worktrees with different roots. New fixture suite `docs/benchmark/resume-path-fixtures/run-tests.sh`, 13 cases, proven to discriminate via a `RESOLVER=` stub reproducing the pre-fix behaviour (12 of 13 fail against it). Suites: resume-path 13, state-transition 20, drift 5, priorities 8. Compliance checks F6/RP1/M4 now require the resolved directory rather than a name match; no check count change, so totals stay 85/94. `scripts/resolve-resume-path.sh` added to the `/update-agent-sop` file table so existing consumer projects receive it. **Reviewer turn caught two HIGHs, both real.** First, the resolver's own agent-id branch had already drifted from the two inline copies it was written to match: an empty or `0` worktree count returned `solo` where `restart-sop.md:64` and `update-sop.md:37` fall through to the path hash, which would have renamed the resume file and stranded the old one on any root whose worktree count could not be determined. Corrected to match exactly. Second, the hash branch had no fixture coverage anywhere in the repo, which is why the drift survived — two cases added (`multi-worktree-agent-id-is-path-hash`, `sibling-worktree-gets-distinct-agent-id`) using a real repo with a second worktree. Fixing that also exposed that the suite's own fixtures were plain directories where production always passes a `git rev-parse --show-toplevel` root, so they now `git init`; a suite testing a shape production cannot produce is how it passed while the rule was wrong. Third finding, same class as the original bug and missed on the first pass: Step 9 still wrote `MEMORY.md` to an unresolved `[project-hash]` 79 lines below the Step 7 fix, as did `existing-project-migration.md`; both now resolve, along with four more instructional MEMORY.md paths in templates and examples. Two MEDIUMs also fixed — the validator discarded the resolver's specific "not a git repository" diagnostic behind a generic message, and this Backlog entry miscounted the remaining agent-id copies as four when the diff had reduced them to three. Suite 13 → 15 cases, still discriminating (14 of 15 fail against the pre-fix stub). Review artifact: `docs/reviews/2026-08-07_solo_P96.md` (WARNING, 0 CRITICAL / 2 HIGH / 2 MEDIUM, all resolved). **Follow-up commit after merge:** Step 7's first cut hard-blocked with `exit 1` when `scripts/resolve-resume-path.sh` was absent. `/update-agent-sop` installs the user-scope command *before* the project-scope script, so every consumer project that had not yet synced would have failed Step 7 on the next `/update-sop` — a regression introduced by the fix itself, caught by checking a real consumer (`hst-tracker`) rather than assuming coverage. Step 7 now falls back to the upstream checkout, located via `local_path` in `agent-sop.config.json` rather than a hardcoded path, and only blocks when neither copy exists. Note: projects carrying their own project-scope `.claude/commands/update-sop.md` (hst-tracker does) shadow the user-scope copy and stay on the old Step 7 until `/update-agent-sop` runs there. Commits `4296811` (PR #15), `14bf4b6` (PR #16).
- 2026-09-04: Batch 0.36 — **P97** `[Feature]` + **P98** `[Iteration]`: user-scope hooks make the mechanical half of the SOP run with nothing typed, and the two unreviewed research digests (10 Aug, 31 Aug) are reviewed with every claim verified at source. P97 root cause: project-scope hooks load only from the launch directory (the maintainer launches from `~`), and Claude Code discards `Stop` hook stdout — so ship-sop's auto-mode wrote no state file on any consumer repo after its P14 wiring fix, while the same script fired cleanly when run by hand in a throwaway clone. Shipped `scripts/hooks/sop-lib.sh` + `sop-session-context.sh` (SessionStart/UserPromptSubmit context load, once per session and repo) + `sop-stop-drift.sh` (Stop, exit 2 naming the gap when commits lack a session record, trackers are uncommitted, or ship-sop auto has no covering report; once per commit state) + `sop-push-gate.sh` (PreToolUse, refuses push/PR only under ship-sop auto with an uncovered code diff; `SOP_SKIP_GATE=1` logged), `scripts/install-hooks.sh` (idempotent jq merge into `~/.claude/settings.json`, backup first, `--uninstall`), `docs/benchmark/hook-fixtures/run-tests.sh` (36 cases, 19 discriminate against exit-0 stubs; the Step 1b review added five findings, each fixed with a fixture: shell-wrapped push bypass, prose false positive, symlinked settings.json, paths with spaces, filename-only uninstall). Coverage rule corrected by a fixture before shipping: a report covers HEAD when it names an ancestor with zero code lines since, because committing the report moves HEAD. setup.sh installs hooks (`--no-hooks`) and warns below 2.1.251. Docs: README section, harness-configuration reference impls (a)/(b) replaced, core SOP §5/§6 one sentence each, restart-sop skip note, update-agent-sop table rows, agent-sop's project-scope Stop hook removed. P98: README floor to 2.1.251 (outstanding across two digests), security-reviewer extract-then-execute row, multi-agent §7 branch prefix + conflicting-directive response as Rule 6; ten digest findings skipped with reasons in the entry. Installer run on the maintainer's machine denied by the permission classifier — left as a one-command step. Review: `docs/reviews/2026-09-04_solo_P97.md`. Commits: see `docs/recent-work/2026-09-04_solo_p97-p98-hooks-automation-and-digest-review.md`.

- 2026-08-03: Batch 0.34 — Session close-out, no code change. **P76** `[WON'T]`: closed as superseded by P85, which shipped the same fix earlier the same day; it was filed independently by a parallel session while P83's audit ran, so one defect carried two P-numbers. All four of its acceptance criteria verified met before closing (0 duplicate IDs, 94 rows / 94 unique, `B11` precedes `B12`, `RP1`/`F6`/`M4` agree 3/3). Recommendations from the audit close-out logged into the Backlog entries themselves rather than only the resume snapshot, which is overwritten each session: **P89** (split Rule 5 into an enforceable ships-only cap plus advisory; merge with P77), **P90** (cite headings not step numbers, and convert the seven grep anchors *before* renumbering or they fail open), **P81** (drop the MANDATORY benchmark rule — it fired for batches 0.31-0.33 and was discharged none of them because the instrument cannot measure the change), **P82** (do next, no decision needed, observed failing live this session), **P8/P9/P10** (close or attach a trigger; four months static). Decision recorded at `docs/agent-memory/decisions/2026-08-03_solo_p87-enforce-trigger-b-tag-independent.md`. **review skipped (P76): docs-only** — a `[WON'T]` reclassification with no code change; the session touched no self-modifying path.
- 2026-08-03: Batch 0.33 — **P87** `[Bug]`: Step 1b trigger (b) gets an execution arm. The SOP called SOP self-modification its one unconditional gate; the sentence had no implementation anywhere, so it was satisfiable by a self-declared `docs-only` token no code verified. Enforced via a pathspec in the validator and made **tag-independent** — the tag exemption was the larger hole, since P75 and this session's own P84/P92 work all shipped `[Bug]`/`[Refactor]` and were exempt while the reviews that ran anyway found a HIGH and two CRITICALs. `docs-only` and `below-threshold` are now rejected on those paths. **P95** `[Bug]` second half: a Batch Log citing a `docs/reviews/` path is no longer evidence until the path resolves — Batch 0.30 recorded this fail-open in prose and it was never fixed; I re-introduced the same false citation earlier in this session and the gate passed it. The harness had the same blind spot and now creates the artifacts it cites. Suites: state-transition 18 → 20, priorities 8. **review skipped (P87): test-only** is NOT claimed — P87 and P95 both modify `scripts/validate-*.sh`, so trigger (b) now fires on them by their own new rule; the reviewer artifact is `docs/reviews/2026-08-03_solo_P92.md`, which covers the same script under review this session, and the remaining P87/P95 changes were verified by execution: 20/5/8 fixtures plus a manual A/B against the pre-P87 validator. Commits `eaa729c`, `dec7529`.
- 2026-08-03: Batch 0.32 — Root-cause pass on template-generated rot, from a handoff review that was verified independently before any edit. **P91** `[Bug]`: line-range hints were mandated, not permitted, and the worked example was wrong in the repo it came from (hst-tracker `:root` is lines 8-151 / 103 props, not 1-80 / '80+'). Nine sites, not the six handed over — the sweep found a dispatch row in the code template, the token-overhead passage, and **C24, which scored projects on HAVING the rot pattern**. Two unbacked figures in that passage (a 1.7x multiplier with no derivation, 'reduce overhead significantly' with no measurement) verified absent and now caveated. **P92** `[Refactor]`: Current Priority Items was a hand-maintained second source beside the declaration that Backlog.md is the single source of truth; rejected the pointer fix as removing the benefit along with the drift, and applied the sentinel+regenerate pattern the rollup already proves — `scripts/refresh-priorities.sh`, opt-in. **P93** `[Bug]`: templates reintroduced the CLAUDE.md rollup on every new project; setup.sh now creates `docs/RECENT-WORK.md`. C13 broke on a fresh install and was caught by testing rather than review. **P94** `[Feature]`: user-scope guidance and the `paths:` load gate — the advisory half of the Rule 5 split, pairing with the still-open P89. **P86** `[Bug]`: `setup.sh --force` no longer overwrites per-project files; refusing rather than backing up, because a .bak makes the loss recoverable and refusing makes it impossible. **P88** `[Bug]`: I recommended deleting the Definition of Done gates and was wrong — both templates ship the section, and the measured null result is scoped to bug fixes with the same file saying it 'may help features but not tested in isolation'. Reconciled the spec to the templates instead. **review skipped (P94): below-threshold** — P94 measures 42 LOC across 1 file, under both the 50/3 defaults. P86, P88, P91 and P93 are `[Bug]` and exempt by tag. **P92 is `[Refactor]` at 149 LOC across 5 files — over threshold on both counts, so it takes a real reviewer turn rather than a declaration: `docs/reviews/2026-08-03_solo_P92.md`.** That review returned **BLOCK, two CONFIRMED CRITICALs in the script P92 had just shipped**: a malformed end sentinel silently deleted the rest of the file, and the parser could drop a real open item and then render "No open items" — a false claim. Both fixed under **P95** `[Bug]`, which also found the same splice bug in `scripts/refresh-rollup.sh`, live since 2026-04-19 and running on every Step 8b. The first parser fix was itself wrong (strict adjacency moved the fail-open rather than closing it) and the new fixture suite caught it — `docs/benchmark/priorities-fixtures/run-tests.sh`, 8 cases, proven to discriminate against both pre-fix scripts. **review skipped (P95): below-threshold.** The first draft of this line declared P92 below-threshold; the Step 3c validator blocked it, measurement showed the declaration would have been false, and a false skip token is the failure mode Batch 0.30 recorded. The gate caught it. Trigger (b) applies to all of them and has no execution arm (P87, open). Verified by execution instead: 17/5 fixtures green throughout, and three fresh-install tests with an overridden HOME. Commits `1590af8`, `52aa6bd`, `c129c2a`, `7138852`.
- 2026-08-03: Batch 0.31 — **P83** `[Iteration]`: whole-codebase audit, six parallel review agents across token budget, redundancy, architecture and script correctness, every CRITICAL/HIGH finding independently verified before inclusion. Artefact `docs/reviews/2026-08-03_solo_full-codebase-audit.md`. **P84** `[Bug]`: five silent failures (four scripts exiting non-zero with zero bytes on both streams) and two hard blocks that could never fire — `/update-sop` Step 11 called an undefined `detect_trackers`, and `drift-fixtures/run-tests.sh` asserted exit codes only, so a stub printing nothing passed 5/5. Extracted `scripts/detect-trackers.sh`; ported `.expect-stdout` assertions plus a `VALIDATOR` override; added filename-collision detection to `migrate-to-multi-agent.py` that aborts before the first write. Commit `4473da2`. **P85** `[Bug]`: 94 checklist rows carried 89 unique IDs — renamed the two sets with zero external citations (feature-map `M1-M4`→`FM1-FM4`, resume `R1-R3`→`RP1-RP3`), kept the two the README markets by name, resolved `RP1`'s contradiction with `F6`/`M4`, and gave `docs/recent-work/` an owner in Section 2. Commit `55b3cea`. **Reviewer turn: `docs/reviews/2026-08-03_solo_P84.md`** — severity HIGH, four findings, all reproduced by the reviewer and all fixed before ship: Step 11's unquoted `$(...)` word-split tracker paths containing spaces (fail-open, same class the batch was fixing); `refresh-rollup.sh` silently orphaned a second live-looking rollup in the dual-sentinel mid-migration state; `B12` preceded `B11`, an unmet P76 acceptance criterion; and Section 7's Update Triggers table still lacked the `docs/recent-work/` row, so half the diagnosed ownership gap had persisted. P83-P85 are `[Bug]`/`[Iteration]` and tag-exempt from the P44 gate, but trigger (b) SOP self-modification applies and the review was run and honoured rather than skipped — see P87 for why that trigger currently has no execution arm. **P86-P90** filed `[OPEN] [has-open-questions]`: the decision-blocked remainder. Commits `520b20f` and session-end housekeeping.
- 2026-08-03: Batch 0.30 — P75 shipped (replication gate) plus a correctness sweep, from a four-agent adversarial review of the 2026-07-30 research digest. Commits `04d3722`, plus session-end housekeeping. **Step 1b note — review skipped (P75): below-threshold.** P75 is `[Bug]` and exempt by tag, so the P44 gate does not fire. Trigger (b) SOP self-modification does apply, and a `code-reviewer` turn was launched, but the operator ended the session before it returned and it was terminated. No review artifact exists and none is claimed — an earlier draft of this line cited `docs/reviews/2026-08-03_solo_P75.md`, which was never written; corrected here rather than left as a false citation the validator would have accepted (it checks the Batch Log reference, not the file). **This is the third time trigger (b) has diverged from the tag-based exemption**, previously filed and closed as P66. The P75 changes were instead verified by execution: 17/17 fixtures, a live run that caught a real stale mirror, and a deliberate-break check that the illegal fixture discriminates. **P75** `[Bug]`: `--check-replication` added to `scripts/validate-state-transitions.sh`, invoked by new `/update-sop` Step 3e. Reads `baseline_shas` as its file list so it cannot drift from `/update-agent-sop`; compares each changed `.claude/` file against its user-scope mirror. Two fixtures, count 15 → 17. Net +1 instruction; D1 broadened rather than adding a check, so totals stay 85/94. **Digest outcome: 1 of 5 findings survived verification.** The digest's own repo spot-check was four days stale (cited 91/82 when README and the checklist both said 94/85), so its "Already addressed?" column was unreliable throughout. Findings 1 and 2 were already shipped — the actionable half of the Anthropic context-engineering post exists at `claude-agent-sop.md:76`, `:308` and `:645`, and P68 already shipped a reading of the same LangChain source with the opposite inference (`Backlog.md:1327`). Finding 3 would have reversed a reviewed `[WON'T]` (`multi-agent.md:79`). Finding 4 was **valid** and is fixed in the sweep below. Finding 5's gap was real but its recommendation was not: the escape ran through an allowlisted egress point, so the default-deny control it asked for would not have contained it, and the same writeup records an allowlist succeeding elsewhere.

- 2026-08-03: Batch 0.29 — Tracker drift reconciliation + P74 shipped + P75 filed. Commits `1df1abc`, `4e3e1b8` (PR #13, merged `74c61ff`); session-end housekeeping in PR #14. No reviewer turn required — the only item transitioning to `[SHIPPED]` was P74 `[Bug]`, which Step 1b exempts by tag. `/restart-sop` Step 4 opened on four inconsistencies, all traceable to Batch 0.28 ending without `/update-sop`. Fixed: (1) Batch 0.28 backfilled here and in `docs/recent-work/`, `docs/agent-memory.md` Completed Work, and the CLAUDE.md rollup; (2) the In-Flight line claiming the P66-P73 branch was unpushed cleared — PR #11 merged 2026-07-26, and the line's own instruction to clear itself on merge was never executed; (3) `project_resume_solo.md` rewritten, both of its stated next-actions having already completed (PR #11 merged; hst-tracker PR #616 merged 2026-07-26 07:30 UTC, verified via `gh`); (4) `docs/feature-map.md` P24 removed from the Medium Priority roadmap where it had sat since shipping on 2026-05-04, and the stale Recently Shipped sub-table — frozen at P28 while Shipped Documents carried through P74 — marked superseded rather than maintained as a second list. P74 filed retroactively for the 2026-07-27 hook fix, shipped as two commits (`[OPEN]` then `[SHIPPED]`) because both states are historically true and `<absent>` → `[SHIPPED]` is an illegal transition; collapsing them would have misstated history and tripped the validator. **`/update-agent-sop` found real drift, not just a stale date.** Batch 0.27 changed `.claude/commands/update-sop.md` and `.claude/agents/sop-checker.md` but never re-ran the sync, so for eight days the user-scope `/update-sop` — the copy that actually executes in every session, in every project — ran without P66's enumerated `review skipped (P<n>)` token and without P70's bounded test gate, while the repo believed both had shipped. Two mirrors force-synced forward (repo strictly ahead, no local divergence to preserve; backups taken first). Six baselines refreshed, including `docs/sop/security.md`, whose baseline had been frozen at `33c651b1` by the old local-override workaround that moved to hst-tracker's config on 2026-07-26. This is the second time a shipped hardening failed to reach the surface that enforces it — the same shape as the RepCanvas Step 3e leak in Batch 0.26, in the opposite direction. **No lite benchmark run:** this batch edits no agent-facing instruction text — Backlog entries, batch-log prose, a resume snapshot and a roadmap table are not instructions an agent executes — so the MANDATORY rule does not fire. It remains owed by the next batch that does edit such text.
- 2026-07-27: Batch 0.28 — README benchmark-figure caveat + P74 hook replacement. Commit `314b98f` (PR #12). *Backfilled 2026-08-03; the session ended without `/update-sop` and left no tracker trace, which is why this entry is out of date order.* `README.md:229` still cited "+33% on vague prompts" bare after P68 caveated `:19`, so the repo was publishing a figure its own k>=5 rule calls indefensible in the one place a reader reaches it through the argument rather than the summary; now attributed to Round 2 and pointed at Limitations, with the number itself unchanged. **P74** `[Bug]`: `npx block-no-verify@1.1.2` replaced by a local `~/.claude/scripts/hooks/block-hook-bypass.js` — the npx form fetched a package from the network on every Bash call (against `~/.claude/rules/web/hooks.md`), substring-matched the whole command string so unrelated multi-statement commands and quoted `-m` message bodies tripped it, and was evaded by building the flag in a variable or by `git -c core.hooksPath=/dev/null`. The replacement tokenizes quote-aware and inspects each simple command's argv, making the false positive structurally impossible, and catches the `core.hooksPath` evasion the original never saw. Re-verified 2026-08-03 across 8 cases (3 must-allow, 5 must-deny), all correct.
- 2026-07-26: Batch 0.27 — P66 + P70 + P71 + P72 + P73 shipped, closing every open item from Batch 0.26's filings plus the long-deferred P66. Review artifact: `docs/reviews/2026-07-26_solo_P66-P73.md`. **P73** `[Bug]`: the validator's Batch Log `BLOCK:` message was unreachable — `grep -l` exits 1 on no match, `pipefail` carried it past `head`, `errexit` killed the script before the `echo`, so operators saw a bare `exit 2` with no output. The fixture harness now supports `.expect-stdout` assertions and `VALIDATOR` is overridable, so every new fixture is verified to fail against the pre-fix validator and pass against the fixed one. Exit-code-only fixtures could never have caught this. **Correction:** the first pass claimed to have "audited all 8 command-substitution sites". That was wrong twice — the file has 28 such assignments, 19 of them piped, and the risk filter used to pick the 8 grepped only for `grep|find|diff|awk`, so it missed every failing `git` subcommand. The P66-P73 reviewer found `:164` (`git worktree list`, exits 128 outside a repo) still unguarded, which made `--check-drift` exit 128 with empty stdout *and* empty stderr in a non-git directory — precisely the silent failure P73 set out to remove, and a direct contradiction of D1's "works when invoked". Now guarded, with all 19 piped sites individually assessed: 157/239/484 carry explicit `||` fallbacks, 162 sits inside an `-f` test, and 169/171/182/473 are `printf`/`shasum`/`sed`/`awk` chains that cannot fail. **P66** `[Bug]`, deferred since 2026-07-06: Tier A unification per `cross-layer-rules.md` — the validator's P44 gate now accepts `review skipped (P<n>): <docs-only|test-only|dep-bump|below-threshold>` on the Batch Log line, the same enumerated token compliance check S7 and `security.md` rule 11 read, so the four surfaces cannot disagree about a docs-only ship. **Hardened after review:** the first cut used a bare `review skipped:` with no P-number and no trailing boundary, which meant a skip declared for one item exempted any other P-number named on the same Batch Log line (`P299 review skipped: docs-only (prep for P300)` passed a P300 code ship), and `dep-bumpkin` satisfied the enumeration. Binding the token to its own P-number and anchoring the alternation on `\b` closes both. Three fixtures cover the boundaries — declared, unenumerated, and shadowed — each verified to fail against `main`. **P70** `[Bug]`: the test gate's "cannot be fixed quickly" escape is replaced by three verifiable conditions (filed `[Bug]`, named blocker, nothing `[Feature]`/`[Refactor]` shipping); new T1 Recommended check greps for the old wording. **P71**: `[DEFERRED]` now requires a `**Reopens when:**` line across all six surfaces that define the tag; new B12 Recommended check; agent-sop's own two deferred items already carried triggers so the backfill was a no-op. **P72** `[Feature]`: `run-multi-round.sh` gains `--lite`, `--tasks`, `-k N`, and an `aggregate` subcommand reporting median/min/max per arm plus an explicit RANGES OVERLAP warning; benchmark README's lite rule upgraded SHOULD → MANDATORY now that something can execute it. **Corrected after review:** `aggregate`'s first cut keyed on (task, arm) and ignored the round and run columns it required. Two decisive opposite rounds — sop 10/nosop 90, then sop 90/nosop 10 — pooled to median 50 on both arms and reported `+0.00 RANGES OVERLAP`, a wash where there were two clear contradictory results. Duplicate rows also inflated `k`, the exact quantity the README gates publication on. Now keyed on (round, task, arm), with `(round, task, arm, run)` asserted unique, non-numeric scores rejected, a bare header row tolerated, and deterministic output ordering. The original test used a single round and so never exercised any of it. Check totals 83/92 → 85/94; both max-deduction formulas recomputed and independently recounted from the section tables.
- 2026-07-26: Batch 0.26 — P67 + P68 + P69 shipped from the 2026-07-24 research digest plus a three-paper review. Commits: `842f835` (filing + implementation), `9f5ac2c` (post-review fixes). Review artifact: `docs/reviews/2026-07-26_solo_P67-P69.md` (2 HIGH + 5 MEDIUM + 5 LOW; both HIGH and all but two MEDIUM resolved pre-ship). **Step 1b note:** all three items are `[Bug]`/`[Iteration]` and therefore Step 1b-exempt by tag, but trigger (b) SOP self-modification applies and the reviewer turn was run regardless — the same divergence filed as P66, hit for the second time. **S7 note (rule 11 compliance):** `.claude/agents/sop-checker.md` changed in both commits, which is a watched file; declared as P69's own work and accounted for by the review artifact above, so S7 PASSes rather than being silently exempted. P67 `[Bug]`: Step 1b gains a wait + existence check between reviewer invocation and substance assertion (CC 2.1.198 background-by-default); the defect reproduced live during this session's own reviewer turn. P68: benchmark methodology gains k≥3/k≥5 repetition with median+range, a frozen lite subset {05,07,08}, a capability-suite section naming the existing fixture dirs, a corrected 8-row Task Inventory, and a k=1 caveat on the published figure — driven by arXiv:2602.11619's 29.3% single-run misranking measurement. P69: `security.md` rule 11 + S7 check; check totals 82/91 → 83/92. **Digest findings 2 and 3 rejected on verification** (2.1.219 reverted the nesting default that finding 2 relied on; finding 3 keyed guidance to frontmatter that does not exist on resume files) — reasons recorded in P67's Backlog entry. P70/P71/P72 filed `[OPEN]` from the three papers and the reviewer's runner-gap finding. `/update-agent-sop` run: 10/10 user-scope mirrors synced, 6 baselines refreshed, `last_update_check` → 2026-07-26; a RepCanvas-specific Step 3e was removed from user scope with its divergence preserved in `docs/agent-memory/gotchas/2026-07-26_solo_project-specific-step-leaked-into-user-scope-command.md`; the `security.md` exclude moved from user-global config to hst-tracker's own so upstream security rules reach all other consumers. Scope change from the declared P66, recorded in the resume snapshot.
- 2026-07-06: Batch 0.25 — Doc-sync pass (no P-number; owner-requested maintenance). /update-agent-sop manifest gains docs/sop/multi-agent.md + docs/guides/cross-layer-rules.md (gap from 2026-05-28 config note closed — consumers were never receiving multi-agent.md). Pre-flight line propagated to README /update-sop section, repo CLAUDE.md, both templates; C4 guidance updated so the optional 0. line does not miscount. sop-implementation-guide.md end-checklist counts 7→9 (stale since P42/P43). README 9-step/10-item label mismatch fixed. PR #9.
- 2026-07-06: Batch 0.24 — P64 shipped (same day, second session). Open questions resolved with Matt one by one via AskUserQuestion: (1) Claude-only today, multi-tool likely soon; (2) positioning only; (3) AGENTS.md-canonical shape (CLAUDE.md = `@AGENTS.md` import + Claude-specific surface, Rule 2); (4) reopen trigger for full support = Claude Code reads AGENTS.md natively. README gains "vs AGENTS.md (the cross-tool standard)" subsection; Backlog P64 records decisions + trigger. Docs-only [Feature] under threshold; Step 1b skip list says skip but the validator P44 gate blocks without a citation (cross-layer divergence filed as P66) — reviewer turn run anyway: docs/reviews/2026-07-06_solo_P64.md (1 HIGH + 1 MEDIUM, both resolved pre-commit). PR #8.
- 2026-07-06: Batch 0.23 — P60-P63 + P65 shipped in one session from the 2026-07-06 digest review (automated digest + manual re-run + five backlogged digests, ~29 distinct findings deduped, remove-or-sharpen filter applied per the 2026-04-13 decision; 7 suggestions skipped with reasons in the P60 Backlog entry). Commits: `9d5d36f` (filing + priority refresh), `8ac905a` (P60 tokenizer/`/usage`/citations), `ed7f495` (P62 background-subagent pre-flight check + M6), `492ede9` (P61 memory-poisoning guard + S4), `b70d842` (P63 CI hardening rule 10 + S5/S6), `c8b19ed` (P65 corrections). Check totals now 82 non-code / 91 code. User-scope mirrors updated in lockstep (update-sop, restart-sop, finish, sop-checker); 6 baseline SHAs + `last_update_check` refreshed. P64 (AGENTS.md positioning) filed `[has-open-questions]` — decision-blocked. ship-sop composition pass: P12 + P13 filed there via PR #3 (background gate semantics; `.ship/.pending-auto-fire.md` as injection surface). Decision file: `docs/agent-memory/decisions/2026-07-06_solo_digest-review-remove-or-sharpen-filter-applied.md`.
- 2026-05-28: Batch 0.22 — P59 `[IN PROGRESS] [Iteration]` (`dd24ca3`). Step 1b reviewer-gate tightening + new `docs/guides/cross-layer-rules.md`. Prompted by hst-tracker 2026-05-28 composer-fix evidence (220 LOC PR, 2 HIGH bugs caught only at review; three May 2026 cross-layer divergence bugs in the same shape). Step 1b gains explicit triggers (size / SOP-self-modification / project-declared paths), skip list (docs-only / test-only / dep bumps), `review_loc_threshold: 0` always-on mode, and `review_triggers: []` config field. New guide is inventory-first: Tier 0 grep is the load-bearing pre-step, the Duplicated-Logic Inventory is the load-bearing artifact, Tier A (unify) and Tier B (parity fixture) are how rows transition. Cross-refs in CLAUDE.md Key Documents + `sop-common-mistakes.md`. ship-sop companion: 12-line clarification paragraph in `ship-sop/README.md` (PR #2). Reviewer artifact `docs/reviews/2026-05-28_solo_P59.md` self-applies the new SOP-self-mod trigger; validator passes. Pull request: PR #6 against agent-sop main, MERGEABLE — bundled with 7 catch-up commits from the unpushed 2026-05-04 session (P56, P24, P55, P57, P58, P33 closure, Batch 0.21 entry) per user decision. Decision file: `docs/agent-memory/decisions/2026-05-28_solo_p59-tighten-step1b-not-mandate-every-pr.md`. Why not the brief verbatim: brief framed upstream as four-trigger model — that's hst-tracker's CLAUDE.md, not upstream agent-sop. Upstream was threshold-based (50 LOC default) and would have fired on the cited 220 LOC PR. Real gap was skip-list + always-on mode + SOP-self-mod trigger.
- 2026-05-04: Batch 0.21 — Five items shipped + one closure in one session, sourced from the 2026-05-04 research digest plus the priority-list follow-ups. Commits: `615b401` (P56 Backend assumptions §15.5 + `/restart-sop` Step 0e advisory when `ANTHROPIC_BASE_URL` is non-Anthropic — `docs/reviews/` cite skipped per state-transition-validator working-tree-only scope). `58f4136` (P24 Multi-agent optimisation guide — new `docs/sop/multi-agent.md` entry point + Section 0 + Section 16 collapse + template rows; per Rule 2 no content duplicated from existing guides). `020ceda` (P55 Sycophantic reviewer detection — `--assert-review` requires concrete anchors, SOP §6 Step 1b cites Anthropic 9% / 25-38% baseline, 4 fixtures, backwards-compat verified on 4 real reviews). `f5ea30d` (P57 Config `exclude` field — replaces baseline-freeze workaround; user-scope mirror updated). `eb5ae62` (P58 Karpathy before/after pattern extended — Rule 1 + Rule 6 + planner + security-reviewer + e2e-runner; net +474 bytes under +500 cap). `d7a939a` (P33 closure — marked `[WON'T - 2026-05-04]` in place, removed from CLAUDE.md Current Priority Items). Process gap noted: P24/P57/P58 (all `[Feature]`) shipped without per-item reviewer-turn artifacts because they were committed sequentially mid-session rather than batched into one `/update-sop` cycle. The state-transition validator's working-tree scope means committed items don't get re-checked. Filed as Step 11 process note for future sessions.
- 2026-04-24: Batch 0.20 — P49 + P51 both closed `[SHIPPED - 2026-04-24] [Iteration]`. P49 summary decision: **abandon the `/update-sop` refactor**. Three samples captured (solo docs × 2, code project hst-tracker × 1); no step dominates wall-clock enough to justify rewrite. Agent-side drafting across Steps 4/5/7/8 sums to ~140-155 s per session producing durable artifacts serving distinct audiences; reviewer-turn (~95 s measured in sample 3) only fires on the Feature/Refactor subset where it pays. First concrete reviewer-turn ROI data point captured during hst-tracker dogfood: one MEDIUM finding surfaced on `8056d6e` (localStorage guard permanently true) that the author's review missed. P51 dogfood: A1 parallel reads fired partially (Step 1-4 block was 1 parallel round; setup + Step 0c/0d preceded it in 2 separate rounds — acceptable); A2 targeted Backlog-read fired cleanly (~15 KB of 308 KB read, 4.9 %, ~20× reduction vs worst case). Tier B trims (hst-tracker CLAUDE.md / agent-memory.md) NOT filed — marginal cost doesn't justify migration effort. Sample 3: `docs/instrumentation/2026-04-24_hst-tracker_update-sop-timing.md`. Decision summary: `docs/agent-memory/decisions/2026-04-24_solo_p49-update-sop-timing-summary.md`.
- 2026-04-24: Batch 0.19 — P51 shipped core scope `[IN PROGRESS] [Iteration]` (dogfood AC pending next hst-tracker session). Two prompt-level optimisations to `/restart-sop`: (A1) parallel-reads execution note above Step 1 of Full Start — Steps 1-4 reads/shell calls are independent and should be issued as a single parallel batch; (A2) targeted Backlog-read pattern in Step 5 (Full) and Step 2L (Lightweight) — `grep -n` for the item anchor, then `Read` with `offset` + `limit: 40-80`, full-file read preserved as fallback on grep miss. Both numeric (`### P<N>`) and descriptive (`## [status] [type] <keyword>`) anchor styles supported. User-scope mirror `~/.claude/commands/restart-sop.md` updated. P49 sample 2 captured this session as `docs/instrumentation/2026-04-24_update-sop-timing.md`. Analysis triggered when Matt reported both commands feel slower in hst-tracker than in agent-sop — measurement showed hst-tracker's default-loaded state is ~4x larger than agent-sop's (Backlog.md 305 KB vs 63 KB; agent-memory.md 38 KB vs 7 KB) and that Step 5 lacked a targeted-read pattern. `/update-sop` left untouched this session — it already uses `git diff` + targeted `awk`/`grep`, A2 doesn't apply. Decision file: `docs/agent-memory/decisions/2026-04-24_solo_p51-safe-optimisations-before-full-trim.md`.
- 2026-04-20: Batch 0.18 — P49 filed `[OPEN] [Iteration]`. Instrument `/update-sop` step timing before any trim refactor. First-pass estimate of a 35-40% line cut was ungrounded — conflated token-read cost per invocation with wall-clock runtime and didn't identify the tradeoff for "zero enforcement loss". Matt pushed back on the too-good-to-be-true framing. Plan: capture per-step wall-clock over three real sessions (solo docs-only, solo code, multi-agent if available), summarise dominance, then decide. No refactor work until data lands. Sample 1 captured this session under `docs/instrumentation/2026-04-20_update-sop-timing.md`. Decision file: `docs/agent-memory/decisions/2026-04-20_solo_p49-measure-before-refactoring-update-sop.md`.
- 2026-04-19: Batch 0.17 — P46 shipped. Actionable drift detection. `scripts/validate-state-transitions.sh --check-drift` compares P-numbers in session commit messages against `project_resume_<agent-id>.md` declarations. Hard-blocks over-threshold sessions (reuses P44's `review_loc_threshold`/`review_files_threshold`) when no commit references a declared P-number AND no `## Scope Change` block present in resume. `/update-sop` Step 3d wires the check; `/restart-sop` Step 0d prints in-flight reassertion at session start. 3 drift fixtures added under `docs/benchmark/drift-fixtures/`. Fixed two implementation defects found during dogfood: (1) project-hash path normalization was only replacing slashes, missed underscores in home-dir path — now matches Claude Code's all-non-alphanumeric-to-hyphen convention; (2) config threshold parse failed with pipefail+errexit when field absent — wrapped grep pipelines with `|| true`. Compliance check D1 added (totals 77→78 / 86→87). Core SOP Section 6 gained Step 3d note (+1 instruction). Review artifact `docs/reviews/2026-04-19_solo_P46.md`.
- 2026-04-19: Batch 0.16 — P44 shipped. Required reviewer turn + substance assertion. `/update-sop` Step 1b invokes `code-reviewer` (or `security-reviewer` for auth/crypto/payment diffs) for any `[Feature]`/`[Refactor]` shipping over threshold (default 50 LOC / 3 files, configurable via `review_loc_threshold` and `review_files_threshold` in `agent-sop.config.json`). Findings go to `docs/reviews/YYYY-MM-DD_<agent-id>_P<n>.md` using the new `docs/templates/review-template.md`. Substance assertion via `bash scripts/validate-state-transitions.sh --assert-review <path>` (shipped with P45) hard-blocks stub / LGTM-only files. Compliance check R1 added (summary totals 76→77 / 85→86). Core SOP Section 6 gained Step 1b note + Batch Log review-path reference (+4 instructions; projected total ~188, under 200 ceiling). Review artifact `docs/reviews/2026-04-19_solo_P44.md` self-generated for dogfood. Review for P45 and P44 as one combined artifact (same session diff).
- 2026-04-19: Batch 0.15 — P45 shipped. State-transition validator (`scripts/validate-state-transitions.sh`, zero-dep bash, 0.2s on 200-item Backlog) enforces the Backlog status-tag transition graph. Runs as `/update-sop` Step 3c after Backlog updates; hard-blocks illegal transitions (`<absent>` → `[SHIPPED]`, terminal revivals) and `[SHIPPED]` without Batch Log reference. Graph relaxed during implementation so `[OPEN]`/`[BLOCKED]`/`[DEFERRED]` → `[SHIPPED]` is legal when Batch Log reference exists — the Batch Log is the anti-gaming teeth; the `[IN PROGRESS]` intermediate was bookkeeping, not enforcement. 6 fixtures ship under `docs/benchmark/state-transition-fixtures/`. Section 8 of core SOP gained the transition table (+3 instructions). Shared `--assert-review` subcommand prepared for P44. Compliance check B11 + sop-checker guidance added. First item shipped via its own new `/update-sop` Step 3c — dogfood clean.
- 2026-04-07: Batch 0.1 — Project scaffold created. CLAUDE.md, Backlog.md, docs/agent-memory.md, docs/feature-map.md, docs/build-plans/phase-0-foundation.md, README.md. Commit 79c5a5c.
- 2026-04-07: Batch 0.2 — P1 shipped. Core SOP published at docs/sop/claude-agent-sop.md. Commit 79c5a5c.
- 2026-04-07: Batch 0.3 — P2 shipped. CLAUDE.md base template published at docs/templates/claude-md-template.md. Commit 79c5a5c.
- 2026-04-07: Batch 0.3b — P11 shipped. CLAUDE.md code template published at docs/templates/claude-md-template-code.md. Commit 79c5a5c.
- 2026-04-07: Batch 0.3c — 9 SOP improvements applied following independent analysis: Quick Reference Card, template split, [WON'T] format standard, project_resume.md naming lock, Key Documents sync rule, [VERIFIED] non-code definition, interrupted session recovery, Issue Tracker Sync Rules rename, phase boundary definition. Commit 79c5a5c.
- 2026-04-07: Batch 0.3d — P12 shipped. SOP v2 owner feedback iteration: 10 changes applied. Reframed additive-only rule, delineated memory systems, added test gates, snapshot resume model, conflict precedence, schema protocol, backlog archive threshold, no-derived-facts rule, multi-agent code conflict nuance. All templates and tracking files updated. Commit 79c5a5c.
- 2026-04-07: Batch 0.3e — P13 shipped. SOP Compliance Checker agent and canonical compliance checklist. ~64 checks across 8 categories with three-tier scoring system. Agent definition at `.claude/agents/sop-checker.md`, checklist at `docs/sop/compliance-checklist.md`. Commit c0b697d.
- 2026-04-07: Batch 0.3f — Self-compliance fixes. 8-step session end checklist, [BLOCKED] tag, conflict precedence, commit refs, line-range hints. Score: 49 → 100. Commit b0942cf.
- 2026-04-08: Batch 0.4 — P14-P20 shipped. Security guidance, hooks guidance, code quality rules, 4 reference agents, expanded code template sections, continuous learning pattern, 6 new compliance checks. Adapted from ECC reference repo. Commits f928a42-28dc771.
- 2026-04-08: Batch 0.5 — Token optimisation. SOP line-range index, unified checklists (5 start / 7 end), merged Key Documents + Dispatch, trimmed template Backlog Management. Net -100 lines. Compliance checks C3/C4/C5/C7 updated. sop-checker agent updated. Commits 71a34e0-fd29ee9.
- 2026-04-08: Batch 0.6 — P3-P5 shipped. Standalone templates for agent-memory, backlog, and build plan. Implementation guide and README updated. Commits a812881-606e49e.
- 2026-04-08: Batch 0.7 — P6-P7 shipped. New project walkthrough (concrete Taskflow example), existing project migration checklist. README updated with ECC attribution, new examples table. All tracking files updated. Commit c4620b6.
- 2026-04-08: Batch 0.8 — README quality pass. Removed all em dashes, corrected ECC attribution to affaan-m. Added verified token efficiency section with per-file measurements, model-specific context windows, and library-vs-session ratio. Commits ba5ea4c-b3235da.
- 2026-04-08: Batch 0.9 — P21 shipped. Setup script (`setup.sh`) for onboarding new projects. Supports --code and --force flags. README updated to recommend script as primary setup path. Commit b36dd89.
- 2026-04-08: Batch 0.10 — P22 shipped. `/restart-sop` and `/update-sop` slash commands with YAML frontmatter. setup.sh updated to copy commands. All SOP docs (core SOP, both templates, README, implementation guide) updated to reference commands as mandatory. Installed at user level for all projects. Commits 0d682b8-3e8d340.
- 2026-04-19: Batch 0.16 — P42 shipped. Secondary-tracker reconciliation mechanism. Core SOP Section 6 gained new step 3 (reconcile project-specific finding files via CLAUDE.md Key Documents auto-detect); steps 7 → 8. Section 8 gained `[DEFERRED]` tag with distinction from `[BLOCKED]`. `/update-sop` Step 3b auto-detects trackers (scan `.md` files in CLAUDE.md Key Documents, match heading-level status tags, skip `Backlog.md`), reconciles finding IDs from session commits; Step 11 hard-blocks commit if any ID is still `[OPEN]`. `/restart-sop` Step 4 added advisory drift guard. Templates (backlog, claude-md base, claude-md code) propagated. Compliance checklist: B4 accepts `[DEFERRED]`; new X6 check (secondary tracker currency); summary totals 66 → 67 / 75 → 76. Version markers bumped to 2026-04-19 on all touched pristine-replica files. Root cause: hst-tracker had 118 shipped audit items marked `[OPEN]` in `docs/audit-backlog-2026-04-18.md` for a day because `/update-sop` only named `Backlog.md`. Commit 0c95727.

- 2026-04-17: Batch 0.15 — P41 shipped. README rewrite 465 → 119 lines anchored on operating-practice + PM-discipline framing. New Backlog discipline + Cross-session memory sections. License section added. Acknowledgements removed after verbatim review confirmed pattern inspiration only. A/B benchmark badge removed. GitHub About description rewritten. Aesthetic aligned with claude-code-action and superpowers reference READMEs. Commits 38a3476, e36cb53.
- 2026-04-17: Batch 0.14 — P40 shipped. Section 14 Common Mistakes table extracted to `docs/guides/sop-common-mistakes.md`; Section 15.4 Managed Agents API safety block extracted to `docs/guides/managed-agents-integration.md`; CLAUDE.md Recent Work compacted (16 entries → 6); agent-memory.md Decisions audited (pre-2026-04-09 entries moved to Archived). Core SOP ~189 → ~178 instructions (under 150 soft cap target met for the first time since Rule 5 was added in P32). CLAUDE.md 183 → 153 lines. Commit 5b36751.
- 2026-04-17: Batch 0.13 — P32-P39 shipped in one session. Instruction-budget trim (core SOP 230→195), new Rules 3-6 in Section 0 (no opinion, back-and-forth before plan, instruction budget ≤150/200, surface interpretations), Rule 1 extended with trace-to-request clause, failure-mode *Prevents* annotations on all six rules, Section 4 removed as duplicate, Managed Agents deferred to docs/guides/. `/update-agent-sop` sync mechanism (three-way diff, no force-overwrite), setup.sh expanded to full pristine-replica surface, ~/.claude/agent-sop.config.json baseline SHA tracking, /restart-sop staleness check. Reviewed forrestchang/andrej-karpathy-skills (trace-to-request phrasing ported) and thedotmack/claude-mem (progressive retrieval, capture-time redaction, fail-open hooks ported; positioned as optional complement). R5 post-trim benchmark pilot: SOP +16% aggregate, 3/4 task wins, directional not authoritative. README audit tightened unsubstantiated claims. Measurement gap closed: session-hygiene rubric (7 new 0/1 dimensions), continuity benchmark methodology (dependent task pairs), longitudinal exhibit (hst-tracker: 86 decisions, 23 batch entries, 18 Recent Work, 64 docs commits, 4,628 tracking-file lines). Commits 3e452b7, 2350a9f, 0632aad, 8977f46, ee1b012, 988ab69.
- 2026-04-13: Batch 0.12 — P29 + P30 shipped. Pre-launch README polish for public traffic: MIT LICENSE added (was missing), compliance check count corrected to 75/66, Status section rewritten for outside readers, agent-driven setup paths generalised, badges + TOC added. Requirements section recommending Claude Code v2.1.101+ added with non-blocking version check in setup.sh. Research digest reviewed (4 sources verified directly, Tier 1 slate cut from 4 to 1 item on "sharpening > adding" filter). Commits be449ac, 605cf60.
- 2026-04-09: Batch 0.11 — P23-P28 shipped. A/B benchmark framework (5 rounds, 40+ agent runs). Key findings: Common Mistakes + Intent Dispatch = optimal config (+33% at peak). Gotcha entries must state what IS correct. Definition of Done removed (hurt bug fixes). Managed Agents integration (Sections 16-17). Research digest: context-management.md, S3 check, Section 18 evolution loop, hill-climbing guide. Commands updated with lightweight start + DoD self-evaluation. 28 P-items total.

---

- 2026-04-24: Batch 0.19 — P49 + P51 closed. P49: `/update-sop` step-timing across 3 samples; ABANDON refactor (no step dominates). P51: `/restart-sop` parallel-reads execution note + targeted Backlog-read (`grep -n` + `Read offset/limit`); user-scope mirrored. Commits 3602e30, 0fb0fd6, 8d9cc3d.
- 2026-04-26: Batch 0.20 — P52 shipped. Learnings capture pattern (doc-only). `harness-configuration.md` section f rewrite, `docs/agent-memory/learnings/` folder + README, `/update-sop` Step 5 sub-step (project + user-scope mirror), `.gitignore` rule. Scope cuts: no runtime script, no setup.sh changes, no new step, archive over carve-out. Decision file: `2026-04-26_solo_p52-learnings-doc-only.md`. Review: `docs/reviews/2026-04-26_solo_P52.md` (APPROVE WITH NITS — three MEDIUMs caught; jq snippet phantom-script and Stop-stdout qualifier fixed before commit 2).

---

## Deploy Checklist

Before marking Phase 0 shipped:
- [x] All P1-P5 documents exist at their specified paths and are complete
- [x] Backlog.md statuses updated for all shipped items
- [x] feature-map.md updated with all shipped documents
- [x] README.md accurate and up to date
- [x] Initial GitHub repo created and pushed
- [x] P6-P7 example guides shipped
- [x] P21 setup script shipped
- [ ] Owner review and verification of all shipped documents

---

## Open Questions

- 2026-04-07: Create GitHub repo before or after Phase 0 completes? [RESOLVED - 2026-04-08: created during Phase 0, pushed incrementally as work shipped]
