# Multi-agent contention rule added to Section 0 — separate branches, merge sequentially, resolve conflicts by appending both entries

**Date:** 2026-04-07
**Agent:** solo

Multi-agent contention rule added to Section 0 — separate branches, merge sequentially, resolve conflicts by appending both entries.

---
*Superseded: pre-cutoff — archived*
